---
description: 完成涉及小程序前端的功能后自动上传新版本
alwaysApply: true
---

# 小程序功能完成后自动上传

当本轮任务**已完成**且改动影响微信小程序客户端（`miniprogram/` 下页面、组件、样式、配置等）时，在收尾阶段**主动上传**体验版，无需再问用户是否上传。

## 版本号

- 在上一已上传版本号上 **patch +1**（如 `1.5.15` → `1.5.16`）。
- 若用户本轮已指定版本号，以用户指定为准。
- 不确定上一版本时，先查近期对话/上传记录，再递增。

## 上传方式

```bash
cd /root/workspace/weixin_miniprogram && node scripts/upload.js <版本号>
```

- 描述备注：**不改**默认功能说明（不要传自定义 desc，除非用户明确要求）。
- 上传前确保相关前端改动已保存；若用户要求提交 git，先提交再上传。
- 纯后端/数据脚本、不影响小程序包的改动：**不必**上传。

## 收尾说明

上传成功后简短告知版本号即可（如「已上传 1.5.16」）。
